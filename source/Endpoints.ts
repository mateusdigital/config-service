class Endpoints {

  static Config = class
  {
    static Get: string = "/config/";
    static Create: string     = "/config"

  };
}


// -----------------------------------------------------------------------------
export default Endpoints;